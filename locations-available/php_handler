# this works only for locations which can be reached
# with their exact name from the web root ..
# i.e. http://.../cloud/ needs to be in /var/www/cloud/ etc.

location ~ \.php/?.*$ {
    include locations-available/php_socket;
}
